// Generated from KiCad symbols by export_components.ts.
import { Component } from "ts-kicad";

/**
 * Audio Jack, 2 Poles (Mono / TS)
 *
 * KiCad symbol: `Connector_Audio:AudioJack2`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle mono phone headphone TS connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack2 extends Component.withPins({
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:AudioJack2";
  override referencePrefix = "J";
}

/**
 * Audio Jack, Dual, 2 Poles (Mono / TS), Grounded Sleeve, Switched Pole (Normalling)
 *
 * KiCad symbol: `Connector_Audio:AudioJack2_Dual_Ground_Switch`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack dual receptacle mono headphones phone TS connector.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack2_Dual_Ground_Switch extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin S1: unnamed; passive. */
  "PS1": "S1",
  /** Physical pin SN1: unnamed; passive. */
  "PSN1": "SN1",
  /** Physical pin T1: unnamed; passive. */
  "PT1": "T1",
  /** Physical pin TN1: unnamed; passive. */
  "PTN1": "TN1",
  /** Physical pin S2: unnamed; passive. */
  "PS2": "S2",
  /** Physical pin SN2: unnamed; passive. */
  "PSN2": "SN2",
  /** Physical pin T2: unnamed; passive. */
  "PT2": "T2",
  /** Physical pin TN2: unnamed; passive. */
  "PTN2": "TN2",
}) {
  override schema = "Connector_Audio:AudioJack2_Dual_Ground_Switch";
  override referencePrefix = "J";
}

/**
 * Audio Jack, Dual, 2 Poles (Mono / TS), Switched Pole (Normalling)
 *
 * KiCad symbol: `Connector_Audio:AudioJack2_Dual_Switch`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack dual receptacle mono headphones phone TS connector.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack2_Dual_Switch extends Component.withPins({
  /** Physical pin S1: unnamed; passive. */
  "PS1": "S1",
  /** Physical pin SN1: unnamed; passive. */
  "PSN1": "SN1",
  /** Physical pin T1: unnamed; passive. */
  "PT1": "T1",
  /** Physical pin TN1: unnamed; passive. */
  "PTN1": "TN1",
  /** Physical pin S2: unnamed; passive. */
  "PS2": "S2",
  /** Physical pin SN2: unnamed; passive. */
  "PSN2": "SN2",
  /** Physical pin T2: unnamed; passive. */
  "PT2": "T2",
  /** Physical pin TN2: unnamed; passive. */
  "PTN2": "TN2",
}) {
  override schema = "Connector_Audio:AudioJack2_Dual_Switch";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 2 Poles (Mono / TS), Grounded Sleeve
 *
 * KiCad symbol: `Connector_Audio:AudioJack2_Ground`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle mono phone headphone TS connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack2_Ground extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:AudioJack2_Ground";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 2 Poles (Mono / TS), Grounded Sleeve, Switched Pole (Normalling)
 *
 * KiCad symbol: `Connector_Audio:AudioJack2_Ground_Switch`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle mono headphones phone TS connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack2_Ground_Switch extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:AudioJack2_Ground_Switch";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 2 Poles (Mono / TS), Grounded Sleeve, Switched T Pole (Normalling)
 *
 * KiCad symbol: `Connector_Audio:AudioJack2_Ground_SwitchT`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle mono headphones phone TS connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack2_Ground_SwitchT extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:AudioJack2_Ground_SwitchT";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 2 Poles (Mono / TS), Switched Pole (Normalling)
 *
 * KiCad symbol: `Connector_Audio:AudioJack2_Switch`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle mono headphones phone TS connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack2_Switch extends Component.withPins({
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:AudioJack2_Switch";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 2 Poles (Mono / TS), Switched T Pole (Normalling)
 *
 * KiCad symbol: `Connector_Audio:AudioJack2_SwitchT`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle mono headphones phone TS connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack2_SwitchT extends Component.withPins({
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:AudioJack2_SwitchT";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 3 Poles (Stereo / TRS)
 *
 * KiCad symbol: `Connector_Audio:AudioJack3`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle stereo headphones phones TRS connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack3 extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:AudioJack3";
  override referencePrefix = "J";
}

/**
 * Audio Jack, Dual, 3 Poles (Stereo / TRS), Grounded Sleeve, Switched Poles (Normalling)
 *
 * KiCad symbol: `Connector_Audio:AudioJack3_Dual_Ground_Switch`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack dual receptacle stereo headphones phones TRS connector.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack3_Dual_Ground_Switch extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R1: unnamed; passive. */
  "PR1": "R1",
  /** Physical pin RN1: unnamed; passive. */
  "PRN1": "RN1",
  /** Physical pin S1: unnamed; passive. */
  "PS1": "S1",
  /** Physical pin SN1: unnamed; passive. */
  "PSN1": "SN1",
  /** Physical pin T1: unnamed; passive. */
  "PT1": "T1",
  /** Physical pin TN1: unnamed; passive. */
  "PTN1": "TN1",
  /** Physical pin R2: unnamed; passive. */
  "PR2": "R2",
  /** Physical pin RN2: unnamed; passive. */
  "PRN2": "RN2",
  /** Physical pin S2: unnamed; passive. */
  "PS2": "S2",
  /** Physical pin SN2: unnamed; passive. */
  "PSN2": "SN2",
  /** Physical pin T2: unnamed; passive. */
  "PT2": "T2",
  /** Physical pin TN2: unnamed; passive. */
  "PTN2": "TN2",
}) {
  override schema = "Connector_Audio:AudioJack3_Dual_Ground_Switch";
  override referencePrefix = "J";
}

/**
 * Audio Jack, Dual, 3 Poles (Stereo / TRS), Switched Poles (Normalling)
 *
 * KiCad symbol: `Connector_Audio:AudioJack3_Dual_Switch`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack dual receptacle stereo headphones phones TRS connector.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack3_Dual_Switch extends Component.withPins({
  /** Physical pin R1: unnamed; passive. */
  "PR1": "R1",
  /** Physical pin RN1: unnamed; passive. */
  "PRN1": "RN1",
  /** Physical pin S1: unnamed; passive. */
  "PS1": "S1",
  /** Physical pin SN1: unnamed; passive. */
  "PSN1": "SN1",
  /** Physical pin T1: unnamed; passive. */
  "PT1": "T1",
  /** Physical pin TN1: unnamed; passive. */
  "PTN1": "TN1",
  /** Physical pin R2: unnamed; passive. */
  "PR2": "R2",
  /** Physical pin RN2: unnamed; passive. */
  "PRN2": "RN2",
  /** Physical pin S2: unnamed; passive. */
  "PS2": "S2",
  /** Physical pin SN2: unnamed; passive. */
  "PSN2": "SN2",
  /** Physical pin T2: unnamed; passive. */
  "PT2": "T2",
  /** Physical pin TN2: unnamed; passive. */
  "PTN2": "TN2",
}) {
  override schema = "Connector_Audio:AudioJack3_Dual_Switch";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 3 Poles (Stereo / TRS), Grounded Sleeve
 *
 * KiCad symbol: `Connector_Audio:AudioJack3_Ground`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle stereo headphones phones TRS connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack3_Ground extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:AudioJack3_Ground";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 3 Poles (Stereo / TRS), Grounded Sleeve, Switched Poles (Normalling)
 *
 * KiCad symbol: `Connector_Audio:AudioJack3_Ground_Switch`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle stereo headphones phones TRS connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack3_Ground_Switch extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:AudioJack3_Ground_Switch";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 3 Poles (Stereo / TRS), Grounded Sleeve, Switched TR Poles (Normalling)
 *
 * KiCad symbol: `Connector_Audio:AudioJack3_Ground_SwitchTR`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle stereo headphones phones TRS connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack3_Ground_SwitchTR extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:AudioJack3_Ground_SwitchTR";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 3 Poles (Stereo / TRS), Switched Poles (Normalling)
 *
 * KiCad symbol: `Connector_Audio:AudioJack3_Switch`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle stereo headphones connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack3_Switch extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:AudioJack3_Switch";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 3 Poles (Stereo / TRS), Switched T Pole (Normalling)
 *
 * KiCad symbol: `Connector_Audio:AudioJack3_SwitchT`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle stereo headphones phones TRS connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack3_SwitchT extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:AudioJack3_SwitchT";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 3 Poles (Stereo / TRS), Switched TR Poles (Normalling)
 *
 * KiCad symbol: `Connector_Audio:AudioJack3_SwitchTR`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle stereo headphones phones TRS connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack3_SwitchTR extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:AudioJack3_SwitchTR";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 4 Poles (TRRS)
 *
 * KiCad symbol: `Connector_Audio:AudioJack4`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle stereo headphones TRRS connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack4 extends Component.withPins({
  /** Physical pin R1: unnamed; passive. */
  "PR1": "R1",
  /** Physical pin R2: unnamed; passive. */
  "PR2": "R2",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:AudioJack4";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 4 Poles (TRRS), Grounded Sleeve
 *
 * KiCad symbol: `Connector_Audio:AudioJack4_Ground`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle stereo headphones TRRS connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack4_Ground extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R1: unnamed; passive. */
  "PR1": "R1",
  /** Physical pin R2: unnamed; passive. */
  "PR2": "R2",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:AudioJack4_Ground";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 4 Poles (Stereo / TRRS), Grounded Sleeve, Switched TR1 Poles (Normalling)
 *
 * KiCad symbol: `Connector_Audio:AudioJack4_Ground_SwitchTR1`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle stereo headphones phones TRS connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack4_Ground_SwitchTR1 extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R1: unnamed; passive. */
  "PR1": "R1",
  /** Physical pin R1N: unnamed; passive. */
  "PR1N": "R1N",
  /** Physical pin R2: unnamed; passive. */
  "PR2": "R2",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:AudioJack4_Ground_SwitchTR1";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 4 Poles (Stereo / TRRS), Switched T1 Pole (Normalling)
 *
 * KiCad symbol: `Connector_Audio:AudioJack4_SwitchT1`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle stereo headphones phones connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack4_SwitchT1 extends Component.withPins({
  /** Physical pin R1: unnamed; passive. */
  "PR1": "R1",
  /** Physical pin R2: unnamed; passive. */
  "PR2": "R2",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:AudioJack4_SwitchT1";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 4 Poles (Stereo / TRRS), Switched TR1 Poles (Normalling)
 *
 * KiCad symbol: `Connector_Audio:AudioJack4_SwitchTR1`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle stereo headphones phones TRS connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack4_SwitchTR1 extends Component.withPins({
  /** Physical pin R1: unnamed; passive. */
  "PR1": "R1",
  /** Physical pin R1N: unnamed; passive. */
  "PR1N": "R1N",
  /** Physical pin R2: unnamed; passive. */
  "PR2": "R2",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:AudioJack4_SwitchTR1";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 5 Poles (TRRRS)
 *
 * KiCad symbol: `Connector_Audio:AudioJack5`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle stereo headphones TRRRS connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack5 extends Component.withPins({
  /** Physical pin R1: unnamed; passive. */
  "PR1": "R1",
  /** Physical pin R2: unnamed; passive. */
  "PR2": "R2",
  /** Physical pin R3: unnamed; passive. */
  "PR3": "R3",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:AudioJack5";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 5 Poles (TRRRS), Grounded Sleeve
 *
 * KiCad symbol: `Connector_Audio:AudioJack5_Ground`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * Keywords: audio jack receptacle stereo headphones TRRRS connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioJack5_Ground extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R1: unnamed; passive. */
  "PR1": "R1",
  /** Physical pin R2: unnamed; passive. */
  "PR2": "R2",
  /** Physical pin R3: unnamed; passive. */
  "PR3": "R3",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:AudioJack5_Ground";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 2 Poles (Mono / TS)
 *
 * KiCad symbol: `Connector_Audio:AudioPlug2`. Reference prefix: `J`.
 * Footprint filters: Plug*.
 * Keywords: audio jack plug stereo headphones TRRS connector 2.5mm 3.5mm 6.35mm.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioPlug2 extends Component.withPins({
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:AudioPlug2";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 3 Poles (Stereo / TRS)
 *
 * KiCad symbol: `Connector_Audio:AudioPlug3`. Reference prefix: `J`.
 * Footprint filters: Plug*.
 * Keywords: audio jack plug stereo headphones TRRS connector 2.5mm 3.5mm 6.35mm.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioPlug3 extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:AudioPlug3";
  override referencePrefix = "J";
}

/**
 * Audio Jack, 4 Poles (Stereo / TRRS)
 *
 * KiCad symbol: `Connector_Audio:AudioPlug4`. Reference prefix: `J`.
 * Footprint filters: Plug*.
 * Keywords: audio jack plug stereo headphones TRRS connector 2.5mm 3.5mm.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class AudioPlug4 extends Component.withPins({
  /** Physical pin R1: unnamed; passive. */
  "PR1": "R1",
  /** Physical pin R2: unnamed; passive. */
  "PR2": "R2",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:AudioPlug4";
  override referencePrefix = "J";
}

/**
 * XLR Connector, Male or Female, 3 Pins
 *
 * KiCad symbol: `Connector_Audio:XLR3`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * Keywords: xlr connector.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class XLR3 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:XLR3";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole female XLR receptacle, grounding: without ground/shell contact, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FAAH`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3faah
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAAH_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAAH extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAAH";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole female XLR receptacle, grounding: without ground/shell contact, horizontal PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC3FAAH-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3faah-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAAH-0_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAAH_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAAH-0";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FAAH1`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3faah1
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAAH1_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAAH1 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAAH1";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, horizontal PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC3FAAH1-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3faah1-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAAH1-0_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAAH1_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAAH1-0";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, horizontal PCB mount, asymmetric push
 *
 * KiCad symbol: `Connector_Audio:NC3FAAH1-DA`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3faah1-da
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAAH1-DA_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAAH1_DA extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAAH1-DA";
  override referencePrefix = "J";
}

/**
 * XLR Connector, Male or Female, 3 Pins, Discrete Ground Pin
 *
 * KiCad symbol: `Connector_Audio:XLR3_Ground`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * Keywords: xlr connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class XLR3_Ground extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:XLR3_Ground";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FAAH2`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3faah2
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAAH2_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAAH2 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3FAAH2";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC3FAAH2-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3faah2-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAAH2-0_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAAH2_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3FAAH2-0";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole female receptacle, grounding: without ground/shell contact, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FAAV`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3faav
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAAV_Vertical.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAAV extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAAV";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole female receptacle, grounding: without ground/shell contact, vertical PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC3FAAV-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3faav-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAAV-0_Vertical.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAAV_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAAV-0";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole female receptacle, grounding: mating connector shell to pin1 and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FAAV1`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3faav1
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAAV1_Vertical.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAAV1 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAAV1";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole female receptacle, grounding: mating connector shell to pin1 and front panel, vertical PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC3FAAV1-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3faav1-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAAV1-0_Vertical.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAAV1_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAAV1-0";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole female receptacle, grounding: mating connector shell to pin1 and front panel, vertical PCB mount, asymmetric push
 *
 * KiCad symbol: `Connector_Audio:NC3FAAV1-DA`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3faav1-da
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAAV1-DA_Vertical.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAAV1_DA extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAAV1-DA";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole female receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FAAV2`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3faav2
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAAV2_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAAV2 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3FAAV2";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole female receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC3FAAV2-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3faav2-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAAV2-0_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAAV2_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3FAAV2-0";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: without ground/shell contact, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FAH`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fah
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAH_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAH extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAH";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: without ground/shell contact, horizontal PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC3FAH-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fah-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAH-0_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAH_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAH-0";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FAH1`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fah1
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAH1_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAH1 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAH1";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, horizontal PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC3FAH1-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fah1-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAH1-0_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAH1_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAH1-0";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, horizontal PCB mount, asymmetric push
 *
 * KiCad symbol: `Connector_Audio:NC3FAH1-DA`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fah1-da
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAH1-DA_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAH1_DA extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAH1-DA";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FAH2`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fah2
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAH2_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAH2 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3FAH2";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC3FAH2-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fah2-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAH2-0_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAH2_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3FAH2-0";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount, asymmetric push
 *
 * KiCad symbol: `Connector_Audio:NC3FAH2-DA`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fah2-da
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAH2-DA_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAH2_DA extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3FAH2-DA";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: without ground/shell contact, lateral left PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC3FAHL-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fahl-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAHL-0_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAHL_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAHL-0";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, lateral left PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FAHL1`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fahl1
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAHL1_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAHL1 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAHL1";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, lateral left PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC3FAHL1-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fahl1-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAHL1-0_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAHL1_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAHL1-0";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: without ground / shell contact, lateral right PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC3FAHR-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fahr-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAHR-0_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAHR_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAHR-0";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, lateral right PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FAHR1`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fahr1
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAHR1_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAHR1 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAHR1";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, lateral right PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC3FAHR1-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fahr1-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAHR1-0_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAHR1_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAHR1-0";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, lateral right PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FAHR2`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fahr2
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAHR2_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAHR2 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3FAHR2";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, lateral right PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC3FAHR2-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fahr2-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAHR2-0_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAHR2_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3FAHR2-0";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: without ground / shell contact, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FAV`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fav
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAV_Vertical.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAV extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAV";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: without ground / shell contact, vertical PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC3FAV-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fav-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAV-0_Vertical.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAV_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAV-0";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FAV1`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fav1
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAV1_Vertical.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAV1 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAV1";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, vertical PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC3FAV1-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fav1-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAV1-0_Vertical.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAV1_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAV1-0";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, vertical PCB mount, asymmetric push
 *
 * KiCad symbol: `Connector_Audio:NC3FAV1-DA`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fav1-da
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAV1-DA_Vertical.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAV1_DA extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FAV1-DA";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FAV2`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fav2
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAV2_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAV2 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3FAV2";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC3FAV2-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fav2-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAV2-0_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAV2_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3FAV2-0";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount, asymmetric push
 *
 * KiCad symbol: `Connector_Audio:NC3FAV2-DA`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fav2-da
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FAV2-DA_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FAV2_DA extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3FAV2-DA";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FBH1`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fbh1
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FBH1_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FBH1 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FBH1";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, horizontal PCB mount, black chrome shell
 *
 * KiCad symbol: `Connector_Audio:NC3FBH1-B`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fbh1-b
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FBH1-B_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FBH1_B extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FBH1-B";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, horizontal PCB mount, asymmetric push
 *
 * KiCad symbol: `Connector_Audio:NC3FBH1-DA`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fbh1-da
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FBH1-DA_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FBH1_DA extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FBH1-DA";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, horizontal PCB mount, additional ground contacts
 *
 * KiCad symbol: `Connector_Audio:NC3FBH1-E`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fbh1-e
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FBH1-E_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FBH1_E extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FBH1-E";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FBH2`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fbh2
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FBH2_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FBH2 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3FBH2";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount, black chrome shell
 *
 * KiCad symbol: `Connector_Audio:NC3FBH2-B`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fbh2-b
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FBH2-B_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FBH2_B extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3FBH2-B";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount, asymmetric push
 *
 * KiCad symbol: `Connector_Audio:NC3FBH2-DA`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fbh2-da
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FBH2-DA_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FBH2_DA extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3FBH2-DA";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount, additional ground contacts
 *
 * KiCad symbol: `Connector_Audio:NC3FBH2-E`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fbh2-e
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FBH2-E_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FBH2_E extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3FBH2-E";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, lateral left PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FBHL1`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fbhl1
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FBHL1_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FBHL1 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FBHL1";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FBV1`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fbv1
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FBV1_Vertical.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FBV1 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FBV1";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, vertical PCB mount, retention spring, no latch
 *
 * KiCad symbol: `Connector_Audio:NC3FBV1-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fbv1-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FBV1-0_Vertical.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FBV1_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FBV1-0";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, vertical PCB mount, black chrome shell
 *
 * KiCad symbol: `Connector_Audio:NC3FBV1-B`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fbv1-b
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FBV1-B_Vertical.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FBV1_B extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FBV1-B";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, vertical PCB mount, asymmetric push
 *
 * KiCad symbol: `Connector_Audio:NC3FBV1-DA`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fbv1-da
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FBV1-DA_Vertical.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FBV1_DA extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3FBV1-DA";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FBV2`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fbv2
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FBV2_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FBV2 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3FBV2";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount, black chrome shell
 *
 * KiCad symbol: `Connector_Audio:NC3FBV2-B`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fbv2-b
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FBV2-B_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FBV2_B extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3FBV2-B";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount, asymmetric push
 *
 * KiCad symbol: `Connector_Audio:NC3FBV2-DA`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fbv2-da
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FBV2-DA_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FBV2_DA extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3FBV2-DA";
  override referencePrefix = "J";
}

/**
 * XLR Connector, Male or Female, 3 Pins, Discrete Ground Pin, SPDT Switch
 *
 * KiCad symbol: `Connector_Audio:XLR3_Ground_Switched`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * Keywords: xlr connector.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class XLR3_Ground_Switched extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin N: unnamed; passive. */
  "PN": "N",
  /** Physical pin NC: unnamed; passive. */
  "PNC": "NC",
  /** Physical pin NO: unnamed; passive. */
  "PNO": "NO",
}) {
  override schema = "Connector_Audio:XLR3_Ground_Switched";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole female XLR receptacle, switching contacts, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3FBV2-SW`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3fbv2-sw
 * Keywords: xlr connector spdt switch neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3FBV2-SW_Vertical.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3FBV2_SW extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin N: unnamed; passive. */
  "PN": "N",
  /** Physical pin NC: unnamed; passive. */
  "PNC": "NC",
  /** Physical pin NO: unnamed; passive. */
  "PNO": "NO",
}) {
  override schema = "Connector_Audio:NC3FBV2-SW";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MAAH`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3maah
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MAAH_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MAAH extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MAAH";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole male XLR receptacle, grounding: ground contact connected to shell ground, but not to front panel and Pin 1, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MAAH-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3maah-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MAAH-0_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MAAH_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MAAH-0";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole male XLR receptacle, grounding: mating connector shell to pin1 and front panel, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MAAH-1`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3maah-1
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MAAH-1_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MAAH_1 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3MAAH-1";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MAAV`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3maav
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MAAV_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MAAV extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MAAV";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole male XLR receptacle, grounding: ground contact connected to shell ground, but not to front panel and Pin 1, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MAAV-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3maav-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MAAV-0_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MAAV_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MAAV-0";
  override referencePrefix = "J";
}

/**
 * AA Series, 3 pole male XLR receptacle, grounding: mating connector shell to pin1 and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MAAV-1`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3maav-1
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MAAV-1_Vertical.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MAAV_1 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3MAAV-1";
  override referencePrefix = "J";
}

/**
 * A Series, Chassis connector H female (A series layout), mates with Phoenix MSTB2,5/3-ST, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MAFH-PH`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mafh-ph
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MAFH-PH_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MAFH_PH extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3MAFH-PH";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MAH`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mah
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MAH_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MAH extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MAH";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole male XLR receptacle, grounding: ground contact connected to shell ground, but not to front panel and Pin 1, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MAH-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mah-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MAH-0_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MAH_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MAH-0";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, lateral left PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MAHL`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mahl
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MAHL_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MAHL extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MAHL";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, lateral right PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MAHR`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mahr
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MAHR_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MAHR extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MAHR";
  override referencePrefix = "J";
}

/**
 * A Series, Chassis connector Phoenix H male (A series layout), mates with Phoenix MSTB 2,5/3-ST, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MAMH-PH`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mamh-ph
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MAMH-PH_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MAMH_PH extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3MAMH-PH";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MAV`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mav
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MAV_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MAV extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MAV";
  override referencePrefix = "J";
}

/**
 * A Series, 3 pole male XLR receptacle, grounding: ground contact connected to shell ground, but not to front panel and Pin 1, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MAV-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mav-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MAV-0_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MAV_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MAV-0";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, steel retention lug, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MBH`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mbh
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MBH_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MBH extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MBH";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole male XLR receptacle, grounding: ground contact connected to shell ground, but not to front panel and Pin 1, steel retention lug, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MBH-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mbh-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MBH-0_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MBH_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MBH-0";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole male XLR receptacle, grounding: mating connector shell to pin1 and front panel, steel retention lug, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MBH-1`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mbh-1
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MBH-1_Horizontal.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MBH_1 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3MBH-1";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, steel retention lug, horizontal PCB mount, black chrome shell
 *
 * KiCad symbol: `Connector_Audio:NC3MBH-B`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mbh-b
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MBH-B_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MBH_B extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MBH-B";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, steel retention lug, horizontal PCB mount, additional ground contacts
 *
 * KiCad symbol: `Connector_Audio:NC3MBH-E`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mbh-e
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MBH-E_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MBH_E extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MBH-E";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, steel retention lug, lateral left PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MBHL`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mbhl
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MBHL_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MBHL extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MBHL";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, steel retention lug, lateral left PCB mount, black chrome shell
 *
 * KiCad symbol: `Connector_Audio:NC3MBHL-B`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mbhl-b
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MBHL-B_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MBHL_B extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MBHL-B";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, steel retention lug, lateral right PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MBHR`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mbhr
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MBHR_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MBHR extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MBHR";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, steel retention lug, lateral right PCB mount, black chrome shell
 *
 * KiCad symbol: `Connector_Audio:NC3MBHR-B`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mbhr-b
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MBHR-B_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MBHR_B extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MBHR-B";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, steel retention lug, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MBV`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mbv
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MBV_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MBV extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MBV";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole male XLR receptacle, grounding: ground contact connected to shell ground, but not to front panel and Pin 1, steel retention lug, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MBV-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mbv-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MBV-0_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MBV_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MBV-0";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole male XLR receptacle, grounding: mating connector shell to pin1 and front panel, steel retention lug, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MBV-1`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mbv-1
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MBV-1_Vertical.
 * Units: 0.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MBV_1 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
}) {
  override schema = "Connector_Audio:NC3MBV-1";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, steel retention lug, vertical PCB mount, black chrome shell
 *
 * KiCad symbol: `Connector_Audio:NC3MBV-B`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mbv-b
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MBV-B_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MBV_B extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MBV-B";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, steel retention lug, vertical PCB mount, additional ground contacts
 *
 * KiCad symbol: `Connector_Audio:NC3MBV-E`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mbv-e
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MBV-E_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MBV_E extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC3MBV-E";
  override referencePrefix = "J";
}

/**
 * B Series, 3 pole male XLR receptacle, switching contacts, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC3MBV-SW`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc3mbv-sw
 * Keywords: xlr connector spdt switch neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC3MBV-SW_Vertical.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC3MBV_SW extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin N: unnamed; passive. */
  "PN": "N",
  /** Physical pin NC: unnamed; passive. */
  "PNC": "NC",
  /** Physical pin NO: unnamed; passive. */
  "PNO": "NO",
}) {
  override schema = "Connector_Audio:NC3MBV-SW";
  override referencePrefix = "J";
}

/**
 * XLR Connector, Male or Female, 4 Pins, Discrete Ground Pin
 *
 * KiCad symbol: `Connector_Audio:XLR4_Ground`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * Keywords: xlr connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class XLR4_Ground extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin 4: unnamed; passive. */
  "P4": "4",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:XLR4_Ground";
  override referencePrefix = "J";
}

/**
 * A Series, 4 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC4FAH`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc4fah
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC4FAH_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC4FAH extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin 4: unnamed; passive. */
  "P4": "4",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC4FAH";
  override referencePrefix = "J";
}

/**
 * A Series, 4 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC4FAH-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc4fah-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC4FAH-0_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC4FAH_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin 4: unnamed; passive. */
  "P4": "4",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC4FAH-0";
  override referencePrefix = "J";
}

/**
 * A Series, 4 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC4FAV`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc4fav
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC4FAV_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC4FAV extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin 4: unnamed; passive. */
  "P4": "4",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC4FAV";
  override referencePrefix = "J";
}

/**
 * A Series, 4 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC4FAV-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc4fav-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC4FAV-0_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC4FAV_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin 4: unnamed; passive. */
  "P4": "4",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC4FAV-0";
  override referencePrefix = "J";
}

/**
 * B Series, 4 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC4FBH`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc4fbh
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC4FBH_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC4FBH extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin 4: unnamed; passive. */
  "P4": "4",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC4FBH";
  override referencePrefix = "J";
}

/**
 * B Series, 4 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC4FBV`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc4fbv
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC4FBV_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC4FBV extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin 4: unnamed; passive. */
  "P4": "4",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC4FBV";
  override referencePrefix = "J";
}

/**
 * A Series, 4 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC4MAH`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc4mah
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC4MAH_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC4MAH extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin 4: unnamed; passive. */
  "P4": "4",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC4MAH";
  override referencePrefix = "J";
}

/**
 * A Series, 4 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC4MAV`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc4mav
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC4MAV_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC4MAV extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin 4: unnamed; passive. */
  "P4": "4",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC4MAV";
  override referencePrefix = "J";
}

/**
 * B Series, 4 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC4MBH`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc4mbh
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC4MBH_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC4MBH extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin 4: unnamed; passive. */
  "P4": "4",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC4MBH";
  override referencePrefix = "J";
}

/**
 * B Series, 4 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC4MBV`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc4mbv
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC4MBV_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC4MBV extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin 4: unnamed; passive. */
  "P4": "4",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC4MBV";
  override referencePrefix = "J";
}

/**
 * XLR Connector, Male or Female, 5 Pins, Discrete Ground Pin
 *
 * KiCad symbol: `Connector_Audio:XLR5_Ground`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * Keywords: xlr connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class XLR5_Ground extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:XLR5_Ground";
  override referencePrefix = "J";
}

/**
 * A Series, 5 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC5FAH`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc5fah
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC5FAH_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC5FAH extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC5FAH";
  override referencePrefix = "J";
}

/**
 * A Series, 5 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount, retention spring instead of latch
 *
 * KiCad symbol: `Connector_Audio:NC5FAH-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc5fah-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC5FAH-0_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC5FAH_0 extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC5FAH-0";
  override referencePrefix = "J";
}

/**
 * A Series, 5 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, horizontal PCB mount, asymmetric push
 *
 * KiCad symbol: `Connector_Audio:NC5FAH-DA`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc5fah-da
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC5FAH-DA_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC5FAH_DA extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC5FAH-DA";
  override referencePrefix = "J";
}

/**
 * A Series, 5 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC5FAV`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc5fav
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC5FAV_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC5FAV extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC5FAV";
  override referencePrefix = "J";
}

/**
 * A Series, 5 pole female XLR receptacle, grounding: mating connector shell to pin1 and front panel, horizontal PCB mount, asymmetric push
 *
 * KiCad symbol: `Connector_Audio:NC5FAV-DA`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc5fav-da
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC5FAV-DA_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC5FAV_DA extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC5FAV-DA";
  override referencePrefix = "J";
}

/**
 * XLR Connector, Male or Female, 5 Pins, Discrete Ground Pin, SPDT Switch
 *
 * KiCad symbol: `Connector_Audio:XLR5_Ground_Switched`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * Keywords: xlr connector.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class XLR5_Ground_Switched extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin N: unnamed; passive. */
  "PN": "N",
  /** Physical pin NC: unnamed; passive. */
  "PNC": "NC",
  /** Physical pin NO: unnamed; passive. */
  "PNO": "NO",
}) {
  override schema = "Connector_Audio:XLR5_Ground_Switched";
  override referencePrefix = "J";
}

/**
 * A Series, 5 pole female XLR receptacle, switching contacts, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount, color coding possible
 *
 * KiCad symbol: `Connector_Audio:NC5FAV-SW`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc5fav-sw
 * Keywords: xlr connector spdt switch neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC5FAV-SW_Vertical.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC5FAV_SW extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin N: unnamed; passive. */
  "PN": "N",
  /** Physical pin NC: unnamed; passive. */
  "PNC": "NC",
  /** Physical pin NO: unnamed; passive. */
  "PNO": "NO",
}) {
  override schema = "Connector_Audio:NC5FAV-SW";
  override referencePrefix = "J";
}

/**
 * B Series, 5 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC5FBH`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc5fbh
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC5FBH_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC5FBH extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC5FBH";
  override referencePrefix = "J";
}

/**
 * B Series, 5 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount, black chrome shell
 *
 * KiCad symbol: `Connector_Audio:NC5FBH-B`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc5fbh-b
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC5FBH-B_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC5FBH_B extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC5FBH-B";
  override referencePrefix = "J";
}

/**
 * B Series, 5 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC5FBV`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc5fbv
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC5FBV_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC5FBV extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC5FBV";
  override referencePrefix = "J";
}

/**
 * B Series, 5 pole female XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount, black chrome shell
 *
 * KiCad symbol: `Connector_Audio:NC5FBV-B`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc5fbv-b
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC5FBV-B_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC5FBV_B extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC5FBV-B";
  override referencePrefix = "J";
}

/**
 * B Series, 5 pole female XLR receptacle, switching contacts, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC5FBV-SW`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc5fbv-sw
 * Keywords: xlr connector spdt switch neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC5FBV-SW_Vertical.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC5FBV_SW extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin N: unnamed; passive. */
  "PN": "N",
  /** Physical pin NC: unnamed; passive. */
  "PNC": "NC",
  /** Physical pin NO: unnamed; passive. */
  "PNO": "NO",
}) {
  override schema = "Connector_Audio:NC5FBV-SW";
  override referencePrefix = "J";
}

/**
 * A Series, 5 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC5MAH`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc5mah
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC5MAH_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC5MAH extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC5MAH";
  override referencePrefix = "J";
}

/**
 * A Series, 5 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC5MAV`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc5mav
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC5MAV_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC5MAV extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC5MAV";
  override referencePrefix = "J";
}

/**
 * A Series, 5 pole male XLR receptacle, switching contacts, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC5MAV-SW`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc5mav-sw
 * Keywords: xlr connector spdt switch neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC5MAV-SW_Vertical.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC5MAV_SW extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin N: unnamed; passive. */
  "PN": "N",
  /** Physical pin NC: unnamed; passive. */
  "PNC": "NC",
  /** Physical pin NO: unnamed; passive. */
  "PNO": "NO",
}) {
  override schema = "Connector_Audio:NC5MAV-SW";
  override referencePrefix = "J";
}

/**
 * B Series, 5 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC5MBH`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc5mbh
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC5MBH_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC5MBH extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC5MBH";
  override referencePrefix = "J";
}

/**
 * B Series, 5 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, horizontal PCB mount, black chrome shell
 *
 * KiCad symbol: `Connector_Audio:NC5MBH-B`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc5mbh-b
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC5MBH-B_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC5MBH_B extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC5MBH-B";
  override referencePrefix = "J";
}

/**
 * B Series, 5 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC5MBV`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc5mbv
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC5MBV_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC5MBV extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC5MBV";
  override referencePrefix = "J";
}

/**
 * B Series, 5 pole male XLR receptacle, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount, black chrome shell
 *
 * KiCad symbol: `Connector_Audio:NC5MBV-B`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc5mbv-b
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC5MBV-B_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC5MBV_B extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
}) {
  override schema = "Connector_Audio:NC5MBV-B";
  override referencePrefix = "J";
}

/**
 * B Series, 5 pole male XLR receptacle, switching contacts, grounding: separate ground contact to mating connector shell and front panel, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NC5MBV-SW`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * @see https://www.neutrik.com/en/product/nc5mbv-sw
 * Keywords: xlr connector spdt switch neutrik.
 * Default footprint: Connector_Audio:Jack_XLR_Neutrik_NC5MBV-SW_Vertical.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NC5MBV_SW extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin N: unnamed; passive. */
  "PN": "N",
  /** Physical pin NC: unnamed; passive. */
  "PNC": "NC",
  /** Physical pin NO: unnamed; passive. */
  "PNO": "NO",
}) {
  override schema = "Connector_Audio:NC5MBV-SW";
  override referencePrefix = "J";
}

/**
 * XLR (Male or Female) and Audio Jack (Stereo / TRS, Switched Poles (Normalling)) Combo Connector, Discrete Switched Ground Pin
 *
 * KiCad symbol: `Connector_Audio:XLR3_AudioJack3_Combo_GroundSwitch_Switch`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * Keywords: xlr connector.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class XLR3_AudioJack3_Combo_GroundSwitch_Switch extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin GN: unnamed; passive. */
  "PGN": "GN",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:XLR3_AudioJack3_Combo_GroundSwitch_Switch";
  override referencePrefix = "J";
}

/**
 * Combo I series, 3 pole XLR female receptacle with 6.35mm (1/4in) switching stereo jack and switching ground contact, horizontal PCB mount, retention spring
 *
 * KiCad symbol: `Connector_Audio:NCJ10FI-H`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj10fi-h
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ10FI-H_Horizontal.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ10FI_H extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin GN: unnamed; passive. */
  "PGN": "GN",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NCJ10FI-H";
  override referencePrefix = "J";
}

/**
 * Combo I series, 3 pole XLR female receptacle with 6.35mm (1/4in) switching stereo jack and switching ground contact, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NCJ10FI-H-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj10fi-h-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ10FI-H-0_Horizontal.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ10FI_H_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin GN: unnamed; passive. */
  "PGN": "GN",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NCJ10FI-H-0";
  override referencePrefix = "J";
}

/**
 * Combo I series, 3 pole XLR female receptacle with 6.35mm (1/4in) switching stereo jack and switching ground contact, vertical PCB mount, retention spring
 *
 * KiCad symbol: `Connector_Audio:NCJ10FI-V`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj10fi-v
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ10FI-V_Vertical.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ10FI_V extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin GN: unnamed; passive. */
  "PGN": "GN",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NCJ10FI-V";
  override referencePrefix = "J";
}

/**
 * Combo I series, 3 pole XLR female receptacle with 6.35mm (1/4in) switching stereo jack and switching ground contact, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NCJ10FI-V-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj10fi-v-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ10FI-V-0_Vertical.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ10FI_V_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin GN: unnamed; passive. */
  "PGN": "GN",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NCJ10FI-V-0";
  override referencePrefix = "J";
}

/**
 * XLR (Male or Female) and Audio Jack (Mono / TS) Combo Connector, Discrete Ground Pin
 *
 * KiCad symbol: `Connector_Audio:XLR3_AudioJack2_Combo_Ground`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * Keywords: xlr connector.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class XLR3_AudioJack2_Combo_Ground extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:XLR3_AudioJack2_Combo_Ground";
  override referencePrefix = "J";
}

/**
 * Combo I series, 3 pole XLR female receptacle with 6.35mm (1/4in) mono jack without switching contact, horizontal PCB mount, retention spring
 *
 * KiCad symbol: `Connector_Audio:NCJ5FI-H`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj5fi-h
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ5FI-H_Horizontal.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ5FI_H extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:NCJ5FI-H";
  override referencePrefix = "J";
}

/**
 * Combo I series, 3 pole XLR female receptacle with 6.35mm (1/4in) mono jack without switching contact, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NCJ5FI-H-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj5fi-h-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ5FI-H-0_Horizontal.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ5FI_H_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:NCJ5FI-H-0";
  override referencePrefix = "J";
}

/**
 * Combo I series, 3 pole XLR female receptacle with 6.35mm (1/4in) mono jack without switching contact, vertical PCB mount, retention spring
 *
 * KiCad symbol: `Connector_Audio:NCJ5FI-V`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj5fi-v
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ5FI-V_Vertical.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ5FI_V extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:NCJ5FI-V";
  override referencePrefix = "J";
}

/**
 * Combo I series, 3 pole XLR female receptacle with 6.35mm (1/4in) mono jack without switching contact, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NCJ5FI-V-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj5fi-v-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ5FI-V-0_Vertical.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ5FI_V_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:NCJ5FI-V-0";
  override referencePrefix = "J";
}

/**
 * XLR (Male or Female) and Audio Jack (Stereo / TRS) Combo Connector, Discrete Ground Pin
 *
 * KiCad symbol: `Connector_Audio:XLR3_AudioJack3_Combo_Ground`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * Keywords: xlr connector.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class XLR3_AudioJack3_Combo_Ground extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:XLR3_AudioJack3_Combo_Ground";
  override referencePrefix = "J";
}

/**
 * Combo A series, 3 pole XLR female receptacle with 6.35mm (1/4in) stereo jack, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NCJ6FA-H`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj6fa-h
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ6FA-H_Horizontal.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ6FA_H extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:NCJ6FA-H";
  override referencePrefix = "J";
}

/**
 * Combo A series, 3 pole XLR female receptacle with 6.35mm (1/4in) stereo jack, horizontal PCB mount, retention spring
 *
 * KiCad symbol: `Connector_Audio:NCJ6FA-H-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj6fa-h-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ6FA-H-0_Horizontal.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ6FA_H_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:NCJ6FA-H-0";
  override referencePrefix = "J";
}

/**
 * Combo A series, 3 pole XLR female receptacle with 6.35mm (1/4in) stereo jack, horizontal PCB mount, asymmetric push
 *
 * KiCad symbol: `Connector_Audio:NCJ6FA-H-DA`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj6fa-h-da
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ6FA-H-DA_Horizontal.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ6FA_H_DA extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:NCJ6FA-H-DA";
  override referencePrefix = "J";
}

/**
 * Combo A series, 3 pole XLR female receptacle with 6.35mm (1/4in) stereo jack, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NCJ6FA-V`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj6fa-v
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ6FA-V_Vertical.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ6FA_V extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:NCJ6FA-V";
  override referencePrefix = "J";
}

/**
 * Combo A series, 3 pole XLR female receptacle with 6.35mm (1/4in) stereo jack, vertical PCB mount, retention spring
 *
 * KiCad symbol: `Connector_Audio:NCJ6FA-V-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj6fa-v-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ6FA-V-0_Vertical.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ6FA_V_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:NCJ6FA-V-0";
  override referencePrefix = "J";
}

/**
 * Combo A series, 3 pole XLR female receptacle with 6.35mm (1/4in) stereo jack, vertical PCB mount, asymmetric push
 *
 * KiCad symbol: `Connector_Audio:NCJ6FA-V-DA`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj6fa-v-da
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ6FA-V-DA_Vertical.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ6FA_V_DA extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:NCJ6FA-V-DA";
  override referencePrefix = "J";
}

/**
 * Combo I series, 3 pole XLR female receptacle with 6.35mm (1/4in) stereo jack without switching contact, horizontal PCB mount, retention spring
 *
 * KiCad symbol: `Connector_Audio:NCJ6FI-H`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj6fi-h
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ6FI-H_Horizontal.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ6FI_H extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:NCJ6FI-H";
  override referencePrefix = "J";
}

/**
 * Combo I series, 3 pole XLR female receptacle with 6.35mm (1/4in) stereo jack without switching contact, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NCJ6FI-H-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj6fi-h-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ6FI-H-0_Horizontal.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ6FI_H_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:NCJ6FI-H-0";
  override referencePrefix = "J";
}

/**
 * Combo I series, 3 pole XLR female receptacle with 6.35mm (1/4in) stereo jack without switching contact, vertical PCB mount, retention spring
 *
 * KiCad symbol: `Connector_Audio:NCJ6FI-V`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj6fi-v
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ6FI-V_Vertical.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ6FI_V extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:NCJ6FI-V";
  override referencePrefix = "J";
}

/**
 * Combo I series, 3 pole XLR female receptacle with 6.35mm (1/4in) stereo jack without switching contact, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NCJ6FI-V-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj6fi-v-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ6FI-V-0_Vertical.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ6FI_V_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:NCJ6FI-V-0";
  override referencePrefix = "J";
}

/**
 * XLR (Male or Female) and Audio Jack (Stereo / TRS, Switched Poles (Normalling)) Combo Connector, Discrete Ground Pin
 *
 * KiCad symbol: `Connector_Audio:XLR3_AudioJack3_Combo_Ground_Switch`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * Keywords: xlr connector.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class XLR3_AudioJack3_Combo_Ground_Switch extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:XLR3_AudioJack3_Combo_Ground_Switch";
  override referencePrefix = "J";
}

/**
 * Combo I series, 3 pole XLR female receptacle with 6.35mm (1/4in) switching stereo jack, horizontal PCB mount, retention spring
 *
 * KiCad symbol: `Connector_Audio:NCJ9FI-H`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj9fi-h
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ9FI-H_Horizontal.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ9FI_H extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NCJ9FI-H";
  override referencePrefix = "J";
}

/**
 * Combo I series, 3 pole XLR female receptacle with 6.35mm (1/4in) switching stereo jack, horizontal PCB mount
 *
 * KiCad symbol: `Connector_Audio:NCJ9FI-H-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj9fi-h-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ9FI-H-0_Horizontal.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ9FI_H_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NCJ9FI-H-0";
  override referencePrefix = "J";
}

/**
 * Combo I series, 3 pole XLR female receptacle with 6.35mm (1/4in) switching stereo jack, vertical PCB mount, retention spring
 *
 * KiCad symbol: `Connector_Audio:NCJ9FI-V`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj9fi-v
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ9FI-V_Vertical.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ9FI_V extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NCJ9FI-V";
  override referencePrefix = "J";
}

/**
 * Combo I series, 3 pole XLR female receptacle with 6.35mm (1/4in) switching stereo jack, vertical PCB mount
 *
 * KiCad symbol: `Connector_Audio:NCJ9FI-V-0`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * @see https://www.neutrik.com/en/product/ncj9fi-v-0
 * Keywords: xlr connector neutrik.
 * Default footprint: Connector_Audio:Jack_XLR-6.35mm_Neutrik_NCJ9FI-V-0_Vertical.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NCJ9FI_V_0 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NCJ9FI-V-0";
  override referencePrefix = "J";
}

/**
 * 6.35mm (1/4 in) Vertical Jack, Non-switching mono jack (T/S)
 *
 * KiCad symbol: `Connector_Audio:NJ2FD-V`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nj2fd-v
 * Keywords: audio jack receptacle mono phone headphone TS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NJ2FD-V_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NJ2FD_V extends Component.withPins({
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:NJ2FD-V";
  override referencePrefix = "J";
}

/**
 * 6.35mm (1/4 in) Vertical Jack, Non-switching stereo jack (T/R/S)
 *
 * KiCad symbol: `Connector_Audio:NJ3FD-V`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nj3fd-v
 * Keywords: audio jack receptacle stereo headphones phones TRS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NJ3FD-V_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NJ3FD_V extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:NJ3FD-V";
  override referencePrefix = "J";
}

/**
 * 6.35mm (1/4 in) Vertical Jack, 2 x switching (normalling) stereo jack (T/TN/R/RN/S)
 *
 * KiCad symbol: `Connector_Audio:NJ5FD-V`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nj5fd-v
 * Keywords: audio jack receptacle stereo headphones phones TRS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NJ5FD-V_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NJ5FD_V extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NJ5FD-V";
  override referencePrefix = "J";
}

/**
 * 6.35mm (1/4 in) Vertical Jack, 3 x switching (normalling) stereo jack (T/TN/R/RN/S/SN)
 *
 * KiCad symbol: `Connector_Audio:NJ6FD-V`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nj6fd-v
 * Keywords: audio jack receptacle stereo headphones connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NJ6FD-V_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NJ6FD_V extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NJ6FD-V";
  override referencePrefix = "J";
}

/**
 * 6.35mm (1/4 in) Vertical Jack, 3 x switching (normalling) stereo jack  B-gauge type (T/TN/R/RN/S/SN)
 *
 * KiCad symbol: `Connector_Audio:NJ6TB-V`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nj6tb-v
 * Keywords: audio jack receptacle stereo headphones connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NJ6TB-V_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NJ6TB_V extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NJ6TB-V";
  override referencePrefix = "J";
}

/**
 * speakON Connector, Male or Female, NL2
 *
 * KiCad symbol: `Connector_Audio:SpeakON_NL2`. Reference prefix: `J`.
 * Footprint filters: Jack*speakON*.
 * @see http://www.neutrik.com/en/speakon/
 * Keywords: speakon connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class SpeakON_NL2 extends Component.withPins({
  /** Physical pin 1-: 1-; passive. */
  "P1__1-": "1-",
  /** Physical pin 1+: 1+; passive. */
  "P1__1+": "1+",
}) {
  override schema = "Connector_Audio:SpeakON_NL2";
  override referencePrefix = "J";
}

/**
 * speakON Chassis Connectors, 2 pole chassis connector, gray D-size flange, countersunk thru holes, horizontal PCB. Replaces NL2MD-H
 *
 * KiCad symbol: `Connector_Audio:NL2MDXX-H-3`. Reference prefix: `J`.
 * Footprint filters: Jack*speakON*.
 * @see https://www.neutrik.com/en/product/nl2mdxx-h-3
 * Keywords: speakon connector neutrik.
 * Default footprint: Connector_Audio:Jack_speakON_Neutrik_NL2MDXX-H-3_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NL2MDXX_H_3 extends Component.withPins({
  /** Physical pin 1-: 1-; passive. */
  "P1__1-": "1-",
  /** Physical pin 1+: 1+; passive. */
  "P1__1+": "1+",
}) {
  override schema = "Connector_Audio:NL2MDXX-H-3";
  override referencePrefix = "J";
}

/**
 * speakON Chassis Connectors, 2 pole chassis connector, black D-size flange, countersunk thru holes, vertical PCB mount. Replaces NL2MD-V
 *
 * KiCad symbol: `Connector_Audio:NL2MDXX-V`. Reference prefix: `J`.
 * Footprint filters: Jack*speakON*.
 * @see https://www.neutrik.com/en/product/nl2mdxx-v
 * Keywords: speakon connector neutrik.
 * Default footprint: Connector_Audio:Jack_speakON_Neutrik_NL2MDXX-V_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NL2MDXX_V extends Component.withPins({
  /** Physical pin 1-: 1-; passive. */
  "P1__1-": "1-",
  /** Physical pin 1+: 1+; passive. */
  "P1__1+": "1+",
}) {
  override schema = "Connector_Audio:NL2MDXX-V";
  override referencePrefix = "J";
}

/**
 * speakON Connector, Male or Female, NL4
 *
 * KiCad symbol: `Connector_Audio:SpeakON_NL4`. Reference prefix: `J`.
 * Footprint filters: Jack*speakON*.
 * @see http://www.neutrik.com/en/speakon/
 * Keywords: speakon connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class SpeakON_NL4 extends Component.withPins({
  /** Physical pin 1-: 1-; passive. */
  "P1__1-": "1-",
  /** Physical pin 2+: 2+; passive. */
  "P2__2+": "2+",
  /** Physical pin 2-: 2-; passive. */
  "P2__2-": "2-",
  /** Physical pin 1+: 1+; passive. */
  "P1__1+": "1+",
}) {
  override schema = "Connector_Audio:SpeakON_NL4";
  override referencePrefix = "J";
}

/**
 * speakON Chassis Connectors, 4 pole chassis connector, black D-size flange, mirrored self tapping screw holes (A-screw), horizontal PCB mount. Replaces NL4MD-H-2
 *
 * KiCad symbol: `Connector_Audio:NL4MDXX-H-2`. Reference prefix: `J`.
 * Footprint filters: Jack*speakON*.
 * @see https://www.neutrik.com/en/product/nl4mdxx-h-2
 * Keywords: speakon connector neutrik.
 * Default footprint: Connector_Audio:Jack_speakON_Neutrik_NL4MDXX-H-2_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NL4MDXX_H_2 extends Component.withPins({
  /** Physical pin 1-: 1-; passive. */
  "P1__1-": "1-",
  /** Physical pin 2+: 2+; passive. */
  "P2__2+": "2+",
  /** Physical pin 2-: 2-; passive. */
  "P2__2-": "2-",
  /** Physical pin 1+: 1+; passive. */
  "P1__1+": "1+",
}) {
  override schema = "Connector_Audio:NL4MDXX-H-2";
  override referencePrefix = "J";
}

/**
 * speakON Chassis Connectors, 4 pole chassis connector, grey D-size flange, self tapping screw holes (A-screw), horizontal PCB mount. Replaces NL4MD-H, NL4MD-V-R, and NL4MD-V-S
 *
 * KiCad symbol: `Connector_Audio:NL4MDXX-H-3`. Reference prefix: `J`.
 * Footprint filters: Jack*speakON*.
 * @see https://www.neutrik.com/en/product/nl4mdxx-h-3
 * Keywords: speakon connector neutrik.
 * Default footprint: Connector_Audio:Jack_speakON_Neutrik_NL4MDXX-H-3_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NL4MDXX_H_3 extends Component.withPins({
  /** Physical pin 1-: 1-; passive. */
  "P1__1-": "1-",
  /** Physical pin 2+: 2+; passive. */
  "P2__2+": "2+",
  /** Physical pin 2-: 2-; passive. */
  "P2__2-": "2-",
  /** Physical pin 1+: 1+; passive. */
  "P1__1+": "1+",
}) {
  override schema = "Connector_Audio:NL4MDXX-H-3";
  override referencePrefix = "J";
}

/**
 * speakON Chassis Connectors, 4 pole chassis connector, black D-size flange, countersunk thru holes, vertical PCB mount. Replaces NL4MD-V, NL4MD-V-R, and NL4MD-V-S
 *
 * KiCad symbol: `Connector_Audio:NL4MDXX-V`. Reference prefix: `J`.
 * Footprint filters: Jack*speakON*.
 * @see https://www.neutrik.com/en/product/nl4mdxx-v
 * Keywords: speakon connector neutrik.
 * Default footprint: Connector_Audio:Jack_speakON_Neutrik_NL4MDXX-V_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NL4MDXX_V extends Component.withPins({
  /** Physical pin 1-: 1-; passive. */
  "P1__1-": "1-",
  /** Physical pin 2+: 2+; passive. */
  "P2__2+": "2+",
  /** Physical pin 2-: 2-; passive. */
  "P2__2-": "2-",
  /** Physical pin 1+: 1+; passive. */
  "P1__1+": "1+",
}) {
  override schema = "Connector_Audio:NL4MDXX-V";
  override referencePrefix = "J";
}

/**
 * speakON Chassis Connectors, 4 pole chassis connector, black D-size flange, mirrored self tapping screw holes (A-screw), vertical PCB mount. Replaces NL4MD-V-2
 *
 * KiCad symbol: `Connector_Audio:NL4MDXX-V-2`. Reference prefix: `J`.
 * Footprint filters: Jack*speakON*.
 * @see https://www.neutrik.com/en/product/nl4mdxx-v-2
 * Keywords: speakon connector neutrik.
 * Default footprint: Connector_Audio:Jack_speakON_Neutrik_NL4MDXX-V-2_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NL4MDXX_V_2 extends Component.withPins({
  /** Physical pin 1-: 1-; passive. */
  "P1__1-": "1-",
  /** Physical pin 2+: 2+; passive. */
  "P2__2+": "2+",
  /** Physical pin 2-: 2-; passive. */
  "P2__2-": "2-",
  /** Physical pin 1+: 1+; passive. */
  "P1__1+": "1+",
}) {
  override schema = "Connector_Audio:NL4MDXX-V-2";
  override referencePrefix = "J";
}

/**
 * speakON Chassis Connectors, 4 pole chassis connector, grey D-size flange, self tapping screw holes (A-screw), vertical PCB mount. Replaces NL4MD-V-1
 *
 * KiCad symbol: `Connector_Audio:NL4MDXX-V-3`. Reference prefix: `J`.
 * Footprint filters: Jack*speakON*.
 * @see https://www.neutrik.com/en/product/nl4mdxx-v-3
 * Keywords: speakon connector neutrik.
 * Default footprint: Connector_Audio:Jack_speakON_Neutrik_NL4MDXX-V-3_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NL4MDXX_V_3 extends Component.withPins({
  /** Physical pin 1-: 1-; passive. */
  "P1__1-": "1-",
  /** Physical pin 2+: 2+; passive. */
  "P2__2+": "2+",
  /** Physical pin 2-: 2-; passive. */
  "P2__2-": "2-",
  /** Physical pin 1+: 1+; passive. */
  "P1__1+": "1+",
}) {
  override schema = "Connector_Audio:NL4MDXX-V-3";
  override referencePrefix = "J";
}

/**
 * speakON Connector, Male or Female, NL8
 *
 * KiCad symbol: `Connector_Audio:SpeakON_NL8`. Reference prefix: `J`.
 * Footprint filters: Jack*speakON*.
 * @see http://www.neutrik.com/en/speakon/
 * Keywords: speakon connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class SpeakON_NL8 extends Component.withPins({
  /** Physical pin 1-: 1-; passive. */
  "P1__1-": "1-",
  /** Physical pin 2+: 2+; passive. */
  "P2__2+": "2+",
  /** Physical pin 2-: 2-; passive. */
  "P2__2-": "2-",
  /** Physical pin 3+: 3+; passive. */
  "P3__3+": "3+",
  /** Physical pin 3-: 3-; passive. */
  "P3__3-": "3-",
  /** Physical pin 4+: 4+; passive. */
  "P4__4+": "4+",
  /** Physical pin 4-: 4-; passive. */
  "P4__4-": "4-",
  /** Physical pin 1+: 1+; passive. */
  "P1__1+": "1+",
}) {
  override schema = "Connector_Audio:SpeakON_NL8";
  override referencePrefix = "J";
}

/**
 * speakON Chassis Connectors, 8 pole chassis connector, nickel metal square G-size flange, countersunk thru holes, vertical PCB mount. Replaces NL8MD-V
 *
 * KiCad symbol: `Connector_Audio:NL8MDXX-V`. Reference prefix: `J`.
 * Footprint filters: Jack*speakON*.
 * @see https://www.neutrik.com/en/product/nl8mdxx-v
 * Keywords: speakon connector neutrik.
 * Default footprint: Connector_Audio:Jack_speakON_Neutrik_NL8MDXX-V_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NL8MDXX_V extends Component.withPins({
  /** Physical pin 1-: 1-; passive. */
  "P1__1-": "1-",
  /** Physical pin 2+: 2+; passive. */
  "P2__2+": "2+",
  /** Physical pin 2-: 2-; passive. */
  "P2__2-": "2-",
  /** Physical pin 3+: 3+; passive. */
  "P3__3+": "3+",
  /** Physical pin 3-: 3-; passive. */
  "P3__3-": "3-",
  /** Physical pin 4+: 4+; passive. */
  "P4__4+": "4+",
  /** Physical pin 4-: 4-; passive. */
  "P4__4-": "4-",
  /** Physical pin 1+: 1+; passive. */
  "P1__1+": "1+",
}) {
  override schema = "Connector_Audio:NL8MDXX-V";
  override referencePrefix = "J";
}

/**
 * speakON Chassis Connectors, 8 pole chassis connector, nickel metal square G-size flange, self tapping screw holes (A-screw), vertical PCB mount. Replaces NL8MD-V-1
 *
 * KiCad symbol: `Connector_Audio:NL8MDXX-V-3`. Reference prefix: `J`.
 * Footprint filters: Jack*speakON*.
 * @see https://www.neutrik.com/en/product/nl8mdxx-v-3
 * Keywords: speakon connector neutrik.
 * Default footprint: Connector_Audio:Jack_speakON_Neutrik_NL8MDXX-V-3_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NL8MDXX_V_3 extends Component.withPins({
  /** Physical pin 1-: 1-; passive. */
  "P1__1-": "1-",
  /** Physical pin 2+: 2+; passive. */
  "P2__2+": "2+",
  /** Physical pin 2-: 2-; passive. */
  "P2__2-": "2-",
  /** Physical pin 3+: 3+; passive. */
  "P3__3+": "3+",
  /** Physical pin 3-: 3-; passive. */
  "P3__3-": "3-",
  /** Physical pin 4+: 4+; passive. */
  "P4__4+": "4+",
  /** Physical pin 4-: 4-; passive. */
  "P4__4-": "4-",
  /** Physical pin 1+: 1+; passive. */
  "P1__1+": "1+",
}) {
  override schema = "Connector_Audio:NL8MDXX-V-3";
  override referencePrefix = "J";
}

/**
 * speakON NL2 (Male or Female) and Audio Jack (Mono / TS) Combo Connector
 *
 * KiCad symbol: `Connector_Audio:SpeakON_NL2_AudioJack2_Combo`. Reference prefix: `J`.
 * Footprint filters: Jack*speakON*.
 * @see http://www.neutrik.com/en/speakon/
 * Keywords: speakon connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class SpeakON_NL2_AudioJack2_Combo extends Component.withPins({
  /** Physical pin 1-: 1-; passive. */
  "P1__1-": "1-",
  /** Physical pin 1+: 1+; passive. */
  "P1__1+": "1+",
}) {
  override schema = "Connector_Audio:SpeakON_NL2_AudioJack2_Combo";
  override referencePrefix = "J";
}

/**
 * speakON Combo, 2 pole combination of speakON socket and 6.35mm (1/4in) jack receptacle, horizontal pcb mount. Replaces NLJ2MD-H
 *
 * KiCad symbol: `Connector_Audio:NLJ2MDXX-H`. Reference prefix: `J`.
 * Footprint filters: Jack*speakON*.
 * @see https://www.neutrik.com/en/product/nlj2mdxx-h
 * Keywords: speakon connector neutrik.
 * Default footprint: Connector_Audio:Jack_speakON-6.35mm_Neutrik_NLJ2MDXX-H_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NLJ2MDXX_H extends Component.withPins({
  /** Physical pin 1-: 1-; passive. */
  "P1__1-": "1-",
  /** Physical pin 1+: 1+; passive. */
  "P1__1+": "1+",
}) {
  override schema = "Connector_Audio:NLJ2MDXX-H";
  override referencePrefix = "J";
}

/**
 * speakON Combo, 2 pole combination of speakON socket and 6.35mm (1/4in) jack receptacle, vertical pcb mount. Replaces NLJ2MD-V
 *
 * KiCad symbol: `Connector_Audio:NLJ2MDXX-V`. Reference prefix: `J`.
 * Footprint filters: Jack*speakON*.
 * @see https://www.neutrik.com/en/product/nlj2mdxx-v
 * Keywords: speakon connector neutrik.
 * Default footprint: Connector_Audio:Jack_speakON-6.35mm_Neutrik_NLJ2MDXX-V_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NLJ2MDXX_V extends Component.withPins({
  /** Physical pin 1-: 1-; passive. */
  "P1__1-": "1-",
  /** Physical pin 1+: 1+; passive. */
  "P1__1+": "1+",
}) {
  override schema = "Connector_Audio:NLJ2MDXX-V";
  override referencePrefix = "J";
}

/**
 * STX Series, 4 pole male chassis connector, metal housing, vertical PCB mount,  self tapping screw holes (A-screw)
 *
 * KiCad symbol: `Connector_Audio:NLT4MD-V`. Reference prefix: `J`.
 * Footprint filters: Jack*speakON*.
 * @see https://www.neutrik.com/en/product/nlt4md-v
 * Keywords: speakon connector neutrik.
 * Default footprint: Connector_Audio:Jack_speakON_Neutrik_NLT4MD-V_Vertical.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NLT4MD_V extends Component.withPins({
  /** Physical pin 1-: 1-; passive. */
  "P1__1-": "1-",
  /** Physical pin 2+: 2+; passive. */
  "P2__2+": "2+",
  /** Physical pin 2-: 2-; passive. */
  "P2__2-": "2-",
  /** Physical pin 1+: 1+; passive. */
  "P1__1+": "1+",
}) {
  override schema = "Connector_Audio:NLT4MD-V";
  override referencePrefix = "J";
}

/**
 * M Series, 6.35mm (1/4in) mono jack, switched, with chrome ferrule and straight PCB pins
 *
 * KiCad symbol: `Connector_Audio:NMJ4HCD2`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nmj4hcd2
 * Keywords: audio jack receptacle mono headphones phone TS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NMJ4HCD2_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NMJ4HCD2 extends Component.withPins({
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NMJ4HCD2";
  override referencePrefix = "J";
}

/**
 * M Series, 6.35mm (1/4in) mono jack, switched, with full threaded nose and straight PCB pins
 *
 * KiCad symbol: `Connector_Audio:NMJ4HFD2`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nmj4hfd2
 * Keywords: audio jack receptacle mono headphones phone TS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NMJ4HFD2_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NMJ4HFD2 extends Component.withPins({
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NMJ4HFD2";
  override referencePrefix = "J";
}

/**
 * M Series, 6.35mm (1/4in) mono jack, switched, with a full threaded nose and offset PCB pins
 *
 * KiCad symbol: `Connector_Audio:NMJ4HFD3`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nmj4hfd3
 * Keywords: audio jack receptacle mono headphones phone TS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NMJ4HFD3_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NMJ4HFD3 extends Component.withPins({
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NMJ4HFD3";
  override referencePrefix = "J";
}

/**
 * M Series, 6.35mm (1/4in) mono jack, switched, with a half threaded nose and offset PCB pins
 *
 * KiCad symbol: `Connector_Audio:NMJ4HHD2`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nmj4hhd2
 * Keywords: audio jack receptacle mono headphones phone TS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NMJ4HHD2_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NMJ4HHD2 extends Component.withPins({
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NMJ4HHD2";
  override referencePrefix = "J";
}

/**
 * M Series, 6.35mm (1/4in) stereo jack, switched, with chrome ferrule and straight PCB pins
 *
 * KiCad symbol: `Connector_Audio:NMJ6HCD2`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nmj6hcd2
 * Keywords: audio jack receptacle stereo headphones connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NMJ6HCD2_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NMJ6HCD2 extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NMJ6HCD2";
  override referencePrefix = "J";
}

/**
 * M Series, 6.35mm (1/4in) stereo jack, switched, with chrome ferrule and offset PCB pins
 *
 * KiCad symbol: `Connector_Audio:NMJ6HCD3`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nmj6hcd3
 * Keywords: audio jack receptacle stereo headphones connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NMJ6HCD3_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NMJ6HCD3 extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NMJ6HCD3";
  override referencePrefix = "J";
}

/**
 * M Series, 6.35mm (1/4in) stereo jack, switched, with full threaded nose and straight PCB pins
 *
 * KiCad symbol: `Connector_Audio:NMJ6HFD2`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nmj6hfd2
 * Keywords: audio jack receptacle stereo headphones connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NMJ6HFD2_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NMJ6HFD2 extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NMJ6HFD2";
  override referencePrefix = "J";
}

/**
 * M Series, 6.35mm (1/4in) stereo jack, switched, full threaded nose, straight PCB pins, gold plated contacts
 *
 * KiCad symbol: `Connector_Audio:NMJ6HFD2-AU`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nmj6hfd2-au
 * Keywords: audio jack receptacle stereo headphones connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NMJ6HFD2-AU_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NMJ6HFD2_AU extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NMJ6HFD2-AU";
  override referencePrefix = "J";
}

/**
 * M Series, 6.35mm (1/4in) stereo jack, switched, with full threaded nose and offset PCB pins
 *
 * KiCad symbol: `Connector_Audio:NMJ6HFD3`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nmj6hfd3
 * Keywords: audio jack receptacle stereo headphones connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NMJ6HFD3_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NMJ6HFD3 extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NMJ6HFD3";
  override referencePrefix = "J";
}

/**
 * Obsolete: M Series, 6.35mm (1/4in) stereo jack, switched, with full threaded nose and fishtail PCB pins
 *
 * KiCad symbol: `Connector_Audio:NMJ6HFD4`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nmj6hfd4
 * Keywords: audio jack receptacle stereo headphones connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NMJ6HFD4_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NMJ6HFD4 extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NMJ6HFD4";
  override referencePrefix = "J";
}

/**
 * M Series, 6.35mm (1/4in) stereo jack, switched, with half threaded nose and offset PCB pins
 *
 * KiCad symbol: `Connector_Audio:NMJ6HHD2`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nmj6hhd2
 * Keywords: audio jack receptacle stereo headphones connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NMJ6HHD2_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NMJ6HHD2 extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NMJ6HHD2";
  override referencePrefix = "J";
}

/**
 * Slim Jacks, 6.35mm (1/4in) stereo jack, unswitched, fully threaded nose, sleeve contact/front panel connection
 *
 * KiCad symbol: `Connector_Audio:NRJ3HF-1`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nrj3hf-1
 * Keywords: audio jack receptacle stereo headphones phones TRS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NRJ3HF-1_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NRJ3HF_1 extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:NRJ3HF-1";
  override referencePrefix = "J";
}

/**
 * Slim Jacks, 6.35mm (1/4in) mono jack, switched, fully threaded nose
 *
 * KiCad symbol: `Connector_Audio:NRJ4HF`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nrj4hf
 * Keywords: audio jack dual receptacle mono headphones phone TS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NRJ4HF_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NRJ4HF extends Component.withPins({
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NRJ4HF";
  override referencePrefix = "J";
}

/**
 * Slim Jacks, 6.35mm (1/4in) mono jack, switched, fully threaded nose, sleeve contact/front panel connection
 *
 * KiCad symbol: `Connector_Audio:NRJ4HF-1`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nrj4hf-1
 * Keywords: audio jack receptacle stereo headphones phones TRS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NRJ4HF-1_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NRJ4HF_1 extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NRJ4HF-1";
  override referencePrefix = "J";
}

/**
 * Slim Jacks, 6.35mm (1/4in) mono jack, switched, half threaded nose
 *
 * KiCad symbol: `Connector_Audio:NRJ4HH`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nrj4hh
 * Keywords: audio jack receptacle mono headphones phone TS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NRJ4HH_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NRJ4HH extends Component.withPins({
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NRJ4HH";
  override referencePrefix = "J";
}

/**
 * Slim Jacks, 6.35mm (1/4in) mono jack, switched, half threaded nose, sleeve contact/front panel connection
 *
 * KiCad symbol: `Connector_Audio:NRJ4HH-1`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nrj4hh-1
 * Keywords: audio jack receptacle mono headphones phone TS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NRJ4HH-1_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NRJ4HH_1 extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NRJ4HH-1";
  override referencePrefix = "J";
}

/**
 * Slim Jacks, 6.35mm (1/4in) stereo jack, switched, fully threaded nose
 *
 * KiCad symbol: `Connector_Audio:NRJ6HF`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nrj6hf
 * Keywords: audio jack receptacle stereo headphones connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NRJ6HF_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NRJ6HF extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NRJ6HF";
  override referencePrefix = "J";
}

/**
 * Slim Jacks, 6.35mm (1/4in) stereo jack, switched, fully threaded nose, sleeve contact/front panel connection
 *
 * KiCad symbol: `Connector_Audio:NRJ6HF-1`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nrj6hf-1
 * Keywords: audio jack receptacle stereo headphones phones TRS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NRJ6HF-1_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NRJ6HF_1 extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NRJ6HF-1";
  override referencePrefix = "J";
}

/**
 * Slim Jacks, 6.35mm (1/4in) stereo jack, switched, gold plated contacts, fully threaded nose, sleeve contact/front panel connection
 *
 * KiCad symbol: `Connector_Audio:NRJ6HF-1-AU`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nrj6hf-1-au
 * Keywords: audio jack receptacle stereo headphones phones TRS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NRJ6HF-1-AU_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NRJ6HF_1_AU extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NRJ6HF-1-AU";
  override referencePrefix = "J";
}

/**
 * Slim Jacks, 6.35mm (1/4in) stereo jack, switched, gold plated contacts, fully threaded nose
 *
 * KiCad symbol: `Connector_Audio:NRJ6HF-AU`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nrj6hf-au
 * Keywords: audio jack receptacle stereo headphones connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NRJ6HF-AU_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NRJ6HF_AU extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NRJ6HF-AU";
  override referencePrefix = "J";
}

/**
 * Slim Jacks, 6.35mm (1/4in) stereo jack, switched, half threaded nose
 *
 * KiCad symbol: `Connector_Audio:NRJ6HH`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nrj6hh
 * Keywords: audio jack receptacle stereo headphones connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NRJ6HH_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NRJ6HH extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NRJ6HH";
  override referencePrefix = "J";
}

/**
 * Slim Jacks, 6.35mm (1/4in) stereo jack, switched, half threaded nose, sleeve contact/front panel connection
 *
 * KiCad symbol: `Connector_Audio:NRJ6HH-1`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nrj6hh-1
 * Keywords: audio jack receptacle stereo headphones phones TRS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NRJ6HH-1_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NRJ6HH_1 extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NRJ6HH-1";
  override referencePrefix = "J";
}

/**
 * Slim Jacks, 6.35mm (1/4in) stereo jack, switched, gold plated contacts, half threaded nose
 *
 * KiCad symbol: `Connector_Audio:NRJ6HH-AU`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nrj6hh-au
 * Keywords: audio jack receptacle stereo headphones connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NRJ6HH-AU_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NRJ6HH_AU extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NRJ6HH-AU";
  override referencePrefix = "J";
}

/**
 * Slim Jacks, 6.35mm (1/4in) stereo jack, metal nose with efficient chassis ground connection, T+R+S normalling contact
 *
 * KiCad symbol: `Connector_Audio:NRJ6HM-1`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nrj6hm-1
 * Keywords: audio jack receptacle stereo headphones connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NRJ6HM-1_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NRJ6HM_1 extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NRJ6HM-1";
  override referencePrefix = "J";
}

/**
 * Slim Jacks, 6.35mm (1/4in) stereo jack, metal nose, gold plated contacts, efficient chassis ground connection, T+R+S normalling contact
 *
 * KiCad symbol: `Connector_Audio:NRJ6HM-1-AU`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nrj6hm-1-au
 * Keywords: audio jack receptacle stereo headphones connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NRJ6HM-1-AU_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NRJ6HM_1_AU extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NRJ6HM-1-AU";
  override referencePrefix = "J";
}

/**
 * Slim Jacks, 6.35mm (1/4in) stereo jack, metal nose, efficient chassis ground connection, T+R normalling contact
 *
 * KiCad symbol: `Connector_Audio:NRJ6HM-1-PRE`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nrj6hm-1-pre
 * Keywords: audio jack receptacle stereo headphones phones TRS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NRJ6HM-1-PRE_Horizontal.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NRJ6HM_1_PRE extends Component.withPins({
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:NRJ6HM-1-PRE";
  override referencePrefix = "J";
}

/**
 * Stacking Jacks, Stereo dual jack, full nose
 *
 * KiCad symbol: `Connector_Audio:NSJ12HC`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nsj12hc
 * Keywords: audio jack dual receptacle stereo headphones phones TRS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NSJ12HC_Horizontal.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NSJ12HC extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R1: unnamed; passive. */
  "PR1": "R1",
  /** Physical pin RN1: unnamed; passive. */
  "PRN1": "RN1",
  /** Physical pin S1: unnamed; passive. */
  "PS1": "S1",
  /** Physical pin SN1: unnamed; passive. */
  "PSN1": "SN1",
  /** Physical pin T1: unnamed; passive. */
  "PT1": "T1",
  /** Physical pin TN1: unnamed; passive. */
  "PTN1": "TN1",
  /** Physical pin R2: unnamed; passive. */
  "PR2": "R2",
  /** Physical pin RN2: unnamed; passive. */
  "PRN2": "RN2",
  /** Physical pin S2: unnamed; passive. */
  "PS2": "S2",
  /** Physical pin SN2: unnamed; passive. */
  "PSN2": "SN2",
  /** Physical pin T2: unnamed; passive. */
  "PT2": "T2",
  /** Physical pin TN2: unnamed; passive. */
  "PTN2": "TN2",
}) {
  override schema = "Connector_Audio:NSJ12HC";
  override referencePrefix = "J";
}

/**
 * Stacking Jacks, Stereo dual jack, full threaded nose
 *
 * KiCad symbol: `Connector_Audio:NSJ12HF-1`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nsj12hf-1
 * Keywords: audio jack dual receptacle stereo headphones phones TRS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NSJ12HF-1_Horizontal.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NSJ12HF_1 extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R1: unnamed; passive. */
  "PR1": "R1",
  /** Physical pin RN1: unnamed; passive. */
  "PRN1": "RN1",
  /** Physical pin S1: unnamed; passive. */
  "PS1": "S1",
  /** Physical pin SN1: unnamed; passive. */
  "PSN1": "SN1",
  /** Physical pin T1: unnamed; passive. */
  "PT1": "T1",
  /** Physical pin TN1: unnamed; passive. */
  "PTN1": "TN1",
  /** Physical pin R2: unnamed; passive. */
  "PR2": "R2",
  /** Physical pin RN2: unnamed; passive. */
  "PRN2": "RN2",
  /** Physical pin S2: unnamed; passive. */
  "PS2": "S2",
  /** Physical pin SN2: unnamed; passive. */
  "PSN2": "SN2",
  /** Physical pin T2: unnamed; passive. */
  "PT2": "T2",
  /** Physical pin TN2: unnamed; passive. */
  "PTN2": "TN2",
}) {
  override schema = "Connector_Audio:NSJ12HF-1";
  override referencePrefix = "J";
}

/**
 * Obsolete: Stacking Jacks, Stereo dual jack, half threaded nose
 *
 * KiCad symbol: `Connector_Audio:NSJ12HH-1`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nsj12hh-1
 * Keywords: audio jack dual receptacle stereo headphones phones TRS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NSJ12HH-1_Horizontal.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NSJ12HH_1 extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R1: unnamed; passive. */
  "PR1": "R1",
  /** Physical pin RN1: unnamed; passive. */
  "PRN1": "RN1",
  /** Physical pin S1: unnamed; passive. */
  "PS1": "S1",
  /** Physical pin SN1: unnamed; passive. */
  "PSN1": "SN1",
  /** Physical pin T1: unnamed; passive. */
  "PT1": "T1",
  /** Physical pin TN1: unnamed; passive. */
  "PTN1": "TN1",
  /** Physical pin R2: unnamed; passive. */
  "PR2": "R2",
  /** Physical pin RN2: unnamed; passive. */
  "PRN2": "RN2",
  /** Physical pin S2: unnamed; passive. */
  "PS2": "S2",
  /** Physical pin SN2: unnamed; passive. */
  "PSN2": "SN2",
  /** Physical pin T2: unnamed; passive. */
  "PT2": "T2",
  /** Physical pin TN2: unnamed; passive. */
  "PTN2": "TN2",
}) {
  override schema = "Connector_Audio:NSJ12HH-1";
  override referencePrefix = "J";
}

/**
 * Obsolete: Stacking Jacks, Stereo dual jack, quick fix nose
 *
 * KiCad symbol: `Connector_Audio:NSJ12HL`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nsj12hl
 * Keywords: audio jack dual receptacle stereo headphones phones TRS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NSJ12HL_Horizontal.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NSJ12HL extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin R1: unnamed; passive. */
  "PR1": "R1",
  /** Physical pin RN1: unnamed; passive. */
  "PRN1": "RN1",
  /** Physical pin S1: unnamed; passive. */
  "PS1": "S1",
  /** Physical pin SN1: unnamed; passive. */
  "PSN1": "SN1",
  /** Physical pin T1: unnamed; passive. */
  "PT1": "T1",
  /** Physical pin TN1: unnamed; passive. */
  "PTN1": "TN1",
  /** Physical pin R2: unnamed; passive. */
  "PR2": "R2",
  /** Physical pin RN2: unnamed; passive. */
  "PRN2": "RN2",
  /** Physical pin S2: unnamed; passive. */
  "PS2": "S2",
  /** Physical pin SN2: unnamed; passive. */
  "PSN2": "SN2",
  /** Physical pin T2: unnamed; passive. */
  "PT2": "T2",
  /** Physical pin TN2: unnamed; passive. */
  "PTN2": "TN2",
}) {
  override schema = "Connector_Audio:NSJ12HL";
  override referencePrefix = "J";
}

/**
 * Obsolete: Stacking Jacks, Mono dual jack, full nose
 *
 * KiCad symbol: `Connector_Audio:NSJ8HC`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nsj8hc
 * Keywords: audio jack dual receptacle mono headphones phone TS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NSJ8HC_Horizontal.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NSJ8HC extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin S1: unnamed; passive. */
  "PS1": "S1",
  /** Physical pin SN1: unnamed; passive. */
  "PSN1": "SN1",
  /** Physical pin T1: unnamed; passive. */
  "PT1": "T1",
  /** Physical pin TN1: unnamed; passive. */
  "PTN1": "TN1",
  /** Physical pin S2: unnamed; passive. */
  "PS2": "S2",
  /** Physical pin SN2: unnamed; passive. */
  "PSN2": "SN2",
  /** Physical pin T2: unnamed; passive. */
  "PT2": "T2",
  /** Physical pin TN2: unnamed; passive. */
  "PTN2": "TN2",
}) {
  override schema = "Connector_Audio:NSJ8HC";
  override referencePrefix = "J";
}

/**
 * Obsolete: Stacking Jacks, Mono dual jack, quick fix nose
 *
 * KiCad symbol: `Connector_Audio:NSJ8HL`. Reference prefix: `J`.
 * Footprint filters: Jack*.
 * @see https://www.neutrik.com/en/product/nsj8hl
 * Keywords: audio jack dual receptacle mono headphones phone TS connector neutrik.
 * Default footprint: Connector_Audio:Jack_6.35mm_Neutrik_NSJ8HL_Horizontal.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class NSJ8HL extends Component.withPins({
  /** Physical pin G: unnamed; passive. */
  "PG": "G",
  /** Physical pin S1: unnamed; passive. */
  "PS1": "S1",
  /** Physical pin SN1: unnamed; passive. */
  "PSN1": "SN1",
  /** Physical pin T1: unnamed; passive. */
  "PT1": "T1",
  /** Physical pin TN1: unnamed; passive. */
  "PTN1": "TN1",
  /** Physical pin S2: unnamed; passive. */
  "PS2": "S2",
  /** Physical pin SN2: unnamed; passive. */
  "PSN2": "SN2",
  /** Physical pin T2: unnamed; passive. */
  "PT2": "T2",
  /** Physical pin TN2: unnamed; passive. */
  "PTN2": "TN2",
}) {
  override schema = "Connector_Audio:NSJ8HL";
  override referencePrefix = "J";
}

/**
 * speakON Connector, Male or Female, NL4, Switched Pins
 *
 * KiCad symbol: `Connector_Audio:SpeakON_NL4_Switch`. Reference prefix: `J`.
 * Footprint filters: Jack*speakON*.
 * @see http://www.neutrik.com/en/speakon/
 * Keywords: speakon connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class SpeakON_NL4_Switch extends Component.withPins({
  /** Physical pin 1-: 1-; passive. */
  "P1__1-": "1-",
  /** Physical pin 1-N: 1-N; passive. */
  "P1_N_1-N": "1-N",
  /** Physical pin 2+: 2+; passive. */
  "P2__2+": "2+",
  /** Physical pin 2+N: 2+N; passive. */
  "P2_N_2+N": "2+N",
  /** Physical pin 2-: 2-; passive. */
  "P2__2-": "2-",
  /** Physical pin 1+: 1+; passive. */
  "P1__1+": "1+",
  /** Physical pin 1+N: 1+N; passive. */
  "P1_N_1+N": "1+N",
  /** Physical pin 2-N: 2-N; passive. */
  "P2_N_2-N": "2-N",
}) {
  override schema = "Connector_Audio:SpeakON_NL4_Switch";
  override referencePrefix = "J";
}

/**
 * XLR (Male or Female) and Audio Jack (Mono / TS) Combo Connector, Discrete Ground Pin
 *
 * KiCad symbol: `Connector_Audio:XLR3_AudioJack2_Combo`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * Keywords: xlr connector.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class XLR3_AudioJack2_Combo extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:XLR3_AudioJack2_Combo";
  override referencePrefix = "J";
}

/**
 * XLR (Male or Female) and Audio Jack (Stereo / TRS) Combo Connector, Discrete Ground Pin
 *
 * KiCad symbol: `Connector_Audio:XLR3_AudioJack3_Combo`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * Keywords: xlr connector.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class XLR3_AudioJack3_Combo extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
}) {
  override schema = "Connector_Audio:XLR3_AudioJack3_Combo";
  override referencePrefix = "J";
}

/**
 * XLR (Male or Female) and Audio Jack (Stereo / TRS, Switched Poles (Normalling)) Combo Connector
 *
 * KiCad symbol: `Connector_Audio:XLR3_AudioJack3_Combo_Switch`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*6.35mm*.
 * Keywords: xlr connector.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class XLR3_AudioJack3_Combo_Switch extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin R: unnamed; passive. */
  "PR": "R",
  /** Physical pin RN: unnamed; passive. */
  "PRN": "RN",
  /** Physical pin S: unnamed; passive. */
  "PS": "S",
  /** Physical pin SN: unnamed; passive. */
  "PSN": "SN",
  /** Physical pin T: unnamed; passive. */
  "PT": "T",
  /** Physical pin TN: unnamed; passive. */
  "PTN": "TN",
}) {
  override schema = "Connector_Audio:XLR3_AudioJack3_Combo_Switch";
  override referencePrefix = "J";
}

/**
 * XLR Connector, Male or Female, 3 Pins, SPDT Switch
 *
 * KiCad symbol: `Connector_Audio:XLR3_Switched`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * Keywords: xlr connector.
 * Units: 2.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class XLR3_Switched extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin N: unnamed; passive. */
  "PN": "N",
  /** Physical pin NC: unnamed; passive. */
  "PNC": "NC",
  /** Physical pin NO: unnamed; passive. */
  "PNO": "NO",
}) {
  override schema = "Connector_Audio:XLR3_Switched";
  override referencePrefix = "J";
}

/**
 * XLR Connector, Male or Female, 4 Pins
 *
 * KiCad symbol: `Connector_Audio:XLR4`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * Keywords: xlr connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class XLR4 extends Component.withPins({
  /** Physical pin 1: unnamed; passive. */
  "P1": "1",
  /** Physical pin 2: unnamed; passive. */
  "P2": "2",
  /** Physical pin 3: unnamed; passive. */
  "P3": "3",
  /** Physical pin 4: unnamed; passive. */
  "P4": "4",
}) {
  override schema = "Connector_Audio:XLR4";
  override referencePrefix = "J";
}

/**
 * XLR Connector, Male or Female, 5 Pins
 *
 * KiCad symbol: `Connector_Audio:XLR5`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * Keywords: xlr connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class XLR5 extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
}) {
  override schema = "Connector_Audio:XLR5";
  override referencePrefix = "J";
}

/**
 * XLR Connector, Male or Female, 6 Pins
 *
 * KiCad symbol: `Connector_Audio:XLR6`. Reference prefix: `J`.
 * Footprint filters: Jack*XLR*.
 * Keywords: xlr connector.
 * Units: 1.
 * Included in BOM: yes.
 * Placed on board: yes.
 */
export class XLR6 extends Component.withPins({
  /** Physical pin 1: P1; passive. */
  "P1": "1",
  /** Physical pin 2: P2; passive. */
  "P2": "2",
  /** Physical pin 3: P3; passive. */
  "P3_3": "3",
  /** Physical pin 4: P3; passive. */
  "P3_4": "4",
  /** Physical pin 5: P5; passive. */
  "P5": "5",
  /** Physical pin 6: P6; passive. */
  "P6": "6",
}) {
  override schema = "Connector_Audio:XLR6";
  override referencePrefix = "J";
}

